<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MaterialAppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">#000</item>
        <item name="materialButtonStyle">@style/DefaultButton</item>
    </style>
    
    <style name="DefaultButton" parent="Widget.MaterialComponents.Button">
        <item name="background">@drawable/background_button_sign_in</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    
    <style name="NavigationMenuStyle">
        <item name="android:textSize">15dp</item>
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@font/century_gothic_400</item>
    </style>

    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/my</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="android:textColor">@color/my</item>
        <item name="android:textAppearance">@color/my</item>
    </style>
</resources>
